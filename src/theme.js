import { createTheme } from "@mui/system";

export const theme = createTheme({
    palette: {
        primary:{
            main: "#27AAE1",
        },
        secondary:{
            main: "#1C75BC",
        },
        otherColor:{
            main:"#FFFFFF",
        }
    }
})